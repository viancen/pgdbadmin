# Server
PORT=3000
NODE_ENV=development

# Session secret (change in production)
SESSION_SECRET=change-this-to-a-random-secret

# Optional: default PostgreSQL port if not specified in login
PG_DEFAULT_PORT=5432
